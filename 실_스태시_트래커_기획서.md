# 실 스태시 트래커 기획서 (1단계 MVP)

## 1. 개요

- **핵심 가치 제안**: 갖고 있는 실 재고를 등록해두면, 그 실로 뜰 수 있는 도안을 역으로 추천받는 개인용 도구
- **범위**: 1단계는 완전히 개인용. 나눔/교환 등 공유 요소는 2단계로 보류
- **핵심 사용 시나리오**: 새 도안을 뜨고 싶을 때 "이미 있는 실 중에 쓸 수 있는 게 있나" 확인하거나, 반대로 "이 실로 뭘 뜰 수 있지" 탐색

---

## 1-1. 기술 스택

- **프론트엔드**: React (Vite 빌드) + React Router — Next.js 아님. 포트폴리오용으로 React+Nest.js 조합과 Next.js 풀스택 조합, 두 프로젝트를 스택으로 구분하기로 결정했고 이 프로젝트는 전자에 해당
- **백엔드**: Nest.js + Prisma
- **DB**: PostgreSQL (Neon — 무료 티어 기간 무제한)
- **배포**: 프론트 Vercel / 백엔드 Render(무료, 15분 유휴 시 슬립) — Railway는 무료 티어 폐지로 제외
- **인증**: JWT를 localStorage에 저장, `Authorization: Bearer` 헤더로 전달 (프론트/백엔드 도메인이 분리돼 있어 쿠키 대신 이 방식 채택 — 근거는 2.9 참고)
- **이미지**: Cloudinary 무료 티어 (사용자 업로드 사진만, 외부 썸네일은 URL만 참조 — 근거는 2.8 참고)
- **외부 API**: Ravelry (도안·실 카탈로그 하이브리드 검색)

---

## 2. 핵심 기능

### 2.1 실 등록
- 브랜드 또는 라인명 입력 시 추천 목록이 드롭다운으로 노출 (도안 자동완성과 동일한 하이브리드 방식: 우리 DB 우선 조회 → 부족하면 Ravelry 실 데이터베이스 API 폴백)
- 클릭 시 브랜드, 소재, 무게 카테고리, 추천 바늘 사이즈가 자동 입력, 클릭하지 않으면 직접 입력한 값 그대로 사용
- 색상명·보유 수량은 사람마다 다르므로 항상 직접 입력
- Ravelry에서 선택한 항목은 최소 메타데이터만 우리 DB(`YarnCatalog`)에 캐싱하여 다음 검색부터 로컬 히트로 처리
- 마스터 정보(Yarn): 브랜드, 실 이름/라인, 색상, 소재, 무게 카테고리(레이스/피어링/DK/워스티드/벌키 등 Ravelry 표준 분류 기준), 추천 바늘 사이즈, 사진(여러 장 첨부 가능), 메모(자유 텍스트, 개인 전용)
- 보유 기록(YarnBatch): 같은 실이라도 여러 타래를 나눠서 등록 가능
  - 염색로트(다이로트) 구분 없이 그냥 수량만 더할 수도 있고
  - 로트 번호별로 각각 따로 등록해서 색 미묘한 차이까지 관리할 수도 있음
  - 타래당 무게/길이는 g·m 또는 oz·yd 중 입력한 대로 받고, 내부적으로는 g·m 기준으로 정규화해서 저장 (표시는 사용자가 입력한 단위 우선)

### 2.2 스태시 목록
- 실 카드 그리드, 무게 카테고리·색상 필터
- 검색창: 브랜드·라인명·색상명으로 실시간 필터링 (도안 탭 검색과 동일한 위치·톤)
- 정렬: 최근 등록순(기본) / 이름순(브랜드+라인명 가나다·알파벳순)
- 소진됨 실은 기본적으로 목록에서 숨김 — "소진됨 보기" 토글로 전환 가능
- 카드에는 총 보유량(여러 배치 합산 야드/그램), 대표 사진(첫 번째 등록 사진)

### 2.3 도안 역추천
- 실 상세 화면에서 "이 실로 뜰 수 있는 도안" 탭
- **소진 처리(`consumed: true`)된 실은 매칭 자체가 실행되지 않음** — 다 쓴 실이 계속 도안을 추천해주는 모순을 막기 위함
- 매칭 기준: 무게 카테고리 일치. 필요 야드(최소 사이즈 기준) 대비 보유 야드 비율(%)을 계산해 모든 결과를 보여주되, 비율 구간별로 칩 색상만 다르게 표시 (여유 있음/충분함/타이트함/부족함) — 100% 미만이라도 숨기지 않고 "조금만 더 있으면 뜰 수 있는" 도안을 놓치지 않게 함
- **게이지 보조 표시**: 실과 도안 둘 다 게이지 정보(콧수/10cm)가 있으면 차이를 비교해서 "게이지 일치" / "게이지 차이 있음" 칩을 추가로 보여줌. 허용 오차는 ±1콧수. 둘 중 하나라도 게이지 정보가 없으면 표시하지 않음 (하드 필터 아님 — 정보성 보조 지표)
- **로트 인식 보조 표시**: 합계 야드는 충분해도 가장 큰 단일 로트만으로는 부족한 경우 "로트를 섞어야 해요" 안내를 추가로 보여줌 (배색 없는 단색 도안 기준 — 로트가 섞이면 색이 미묘하게 달라질 수 있다는 걸 숙련자는 신경 쓰기 때문)
- 데이터 소스는 기존에 설계한 하이브리드 방식 재사용: 우리 DB 우선 조회 → 부족하면 Ravelry API 폴백
- 추천 결과 카드: 도안명, 필요 야드(범위가 있으면 "850~1200m" 형태), 여유분 비율 칩, 게이지/로트 보조 칩(해당 시)

### 2.4 관심 도안 (찜)
- 도안 탭의 도안 목록/상세 어디서든 하트 아이콘으로 바로 찜 가능
- 도안 목록에서 "전체 / 찜함" 필터로 전환
- 찜한 도안에는 개인 메모를 남길 수 있음(예: "5.5mm로 바꿔서 뜰 것") — 메모는 `PatternBookmark`에 저장되는 개인 전용 데이터라 다른 유저에게 노출되지 않음. 찜을 해제하면 메모도 함께 사라짐
- 어떤 실과 매칭되어 찜했는지는 기록하지 않는 단순 북마크

### 2.5 도안 등록 (로컬 DB + Ravelry 통합 검색)
- 도안 탭에서 "+"를 누르면 도안명/작가 검색창이 먼저 뜸 — 우리 DB와 Ravelry 결과를 한 목록에 합쳐서 보여줌 (각 항목에 출처 표시)
- 이미 등록된 도안을 클릭하면 그대로 도안 상세로 이동 (중복 등록 방지)
- Ravelry에만 있는 도안을 클릭하면 도안명·작가·무게 카테고리·필요량(범위가 있으면 최소/최대 둘 다)이 자동으로 채워지고, 확인 후 등록하면 우리 DB에도 캐싱됨 (`sourceType: RAVELRY`로 표시)
- 검색해도 안 나오는 도안(개인 도안, SNS에서 본 도안 등)은 아래 필드를 직접 입력해서 신규 등록 — 참고 링크를 넣으면 `sourceType: LINK`, 링크 없이 순수 입력만 하면 `sourceType: USER`
- 로컬로 등록한 도안은 즉시 검색에 노출되어, 다른 사람이 같은 도안을 찾을 때도 바로 히트 — Ravelry 폴백 의존도를 줄이는 핵심 장치
- 필수 항목: 도안명, 도구 종류(대바늘/코바늘/대바늘+코바늘), 무게 카테고리, 필요량 최소값(숫자 + 단위) — Ravelry에서 자동 채워진 경우에도 이 값들이 비어 있으면 저장 전에 직접 채워야 함 (스키마상 non-null이라 반드시 값이 있어야 저장 가능)
- 선택 항목: 작가, 필요량 최대값(사이즈별로 다른 경우), 원본 실(디자이너가 사용한 실 — 브랜드/라인명 자동완성, `YarnCatalog` 재사용), 참고 링크(블로그·SNS 등 원본 출처)
- 필요량은 m 또는 yd 단위를 선택해서 입력받고, 내부적으로는 m 기준으로 정규화 (실 배치 등록 때와 동일한 원칙)
- 도안 서술 내용은 받지 않고 구조화된 사실 정보만 입력받음 (트래커 프로젝트와 동일한 저작권 원칙)

### 2.6 양방향 매칭
- 실 → 도안: 실 상세에서 "이 실로 뜰 수 있는 도안" (기존)
- 도안 → 실: 도안 상세에서 "내가 가진 실 중 맞는 것" — 같은 무게 카테고리 + 보유 야드가 필요량(최소값) 이상인 실 목록, 소진 처리된 실은 여기서도 제외

### 2.7 실 / 도안 수정 · 삭제
- 실: 마스터 정보(브랜드, 색상, 소재, 무게 카테고리, 바늘 사이즈, 사진, 메모)와 배치(로트)별 수량·무게·길이까지 모두 수정 가능
- 도안: 등록한 사람(`createdByUserId`)만 수정 가능 — Ravelry 출처 도안도 처음 캐싱한 사람이 소유자가 되어 수정할 수 있음. 수정 시 `ravelryId`는 참고용으로 유지
- **삭제**: 실은 소유자가 언제든 통째로 삭제 가능(배치·사진 함께 cascade 삭제, 확인 모달 거침) — 이 실이 어떤 프로젝트에 연결돼 있어도 삭제 자체는 막지 않고, 해당 프로젝트의 실 연결만 해제됨(`onDelete: SetNull`). 도안은 등록한 사람만 삭제 가능하되, **본인 외에 다른 유저가 찜했거나, 이 도안으로 진행 중인 프로젝트가 하나라도 있으면 삭제 자체가 불가** — 삭제 버튼이 비활성화되고 사유에 맞는 안내(찜 존재/프로젝트 존재)를 표시. 잘못된 정보를 고치고 싶으면 삭제 대신 수정 기능을 사용하도록 유도
- 1단계에서는 소프트 삭제(복구 가능) 없이 단순 하드 삭제로 시작 — 배치 삭제 정책과 동일한 원칙 (필요성이 실제로 생기면 그때 확장)

### 2.8 이미지 저장 정책
- **사용자가 직접 찍은 사진**(실 사진, 진행 사진 등)만 Cloudinary 무료 티어(카드 등록 불필요, 월 25 크레딧 = 스토리지·대역폭·변환 합산)에 업로드. 클라이언트에서 Cloudinary로 직접 업로드(unsigned upload preset)하고 반환된 URL만 백엔드에 저장 — Render 무료 티어 리소스를 이미지 처리에 쓰지 않기 위함
- **Ravelry 등 외부 출처의 썸네일**은 절대 다운로드해서 재호스팅하지 않고, 원본 URL 문자열만 `thumbnailUrl`에 저장 — 비용이 전혀 들지 않을 뿐 아니라, 이미지를 복제해 우리 스토리지에 올리는 행위는 Ravelry 이용약관상 도안 관련 자산 재사용 제한 원칙에 저촉될 수 있어 원천적으로 지양
- 이미지 삭제/변환 기능을 대비해 `YarnPhoto.publicId`처럼 Cloudinary `public_id`를 저장할 필드를 함께 둠 (URL만으로는 삭제 API 호출이 번거로움)

### 2.9 인증 & 게스트 모드
- **첫 화면에서 명시적으로 선택**: 기기에 세션 토큰이 없는 최초 실행 시, "카카오로 로그인 / 구글로 로그인 / 게스트로 시작하기"를 한 화면에 나란히 보여줌 — 자동으로 게스트 처리하지 않고 사용자가 인지하고 고르게 함
- "게스트로 시작하기"를 누르면 서버가 익명 게스트 사용자를 생성하고 JWT를 발급, **localStorage에 저장**(쿠키 대신) — 이후에는 실/도안 등록 등 모든 기능을 바로 사용 가능
  - 쿠키 대신 localStorage를 쓰는 이유: 백엔드(Render)와 프론트(Vercel)가 다른 도메인이라 쿠키를 쓰려면 `SameSite=None`이 필요한데, Safari의 서드파티 쿠키 차단이나 카카오톡 인앱 브라우저의 까다로운 쿠키 처리 때문에 안정적으로 동작 안 할 위험이 큼. localStorage + `Authorization: Bearer` 헤더 방식이 도메인 분리 구조에서 더 안정적
  - 매 요청마다 프론트에서 `Authorization: Bearer <token>` 헤더로 직접 실어 보냄
  - 토큰 만료는 길게(1년) 설정 — 민감한 세션이 아니라 "이 기기가 누구인지" 식별용이라, 짧게 만료시키면 며칠 뒤 재방문한 게스트가 데이터를 잃은 것처럼 오해할 수 있음
  - 방치된 게스트 계정 정리는 1단계에서 하지 않음 — 실사용 데이터로 필요성이 확인되면 그때 배치 작업으로 정리 고려
- 이미 세션 토큰이 있는 기기(게스트든 로그인이든)는 이 화면을 다시 보지 않고 바로 메인으로 진입
- **로그인은 언제든 승격 가능**: 게스트로 쓰다가도 "마이" 탭에서 나중에 로그인 가능 — 첫 화면의 선택이 마지막 기회는 아님
- Provider 식별은 이메일이 아닌 `provider + providerId` 조합 기준 (카카오는 이메일 제공 동의를 안 받을 수 있어 이메일 통합은 위험)
- **게스트 → 계정 전환 시 데이터 병합**: 게스트로 쓰다가 로그인하면, 게스트 소유였던 실/찜 데이터를 그대로 로그인한 계정으로 이관 (소유권 재할당, 삭제하지 않음)
- **데이터 소유권 구분** — 이 프로젝트에서 중요한 원칙
  - 개인 소유(`userId` 필요): `Yarn`(내 실 재고), `PatternBookmark`(내 찜)
  - 전체 공유(`userId` 없음, 대신 `createdByUserId` 기록): `Pattern`(도안 DB), `YarnCatalog`(실 카탈로그) — UGC 취지상 유저별로 분리하면 안 됨
- **공유 데이터 쓰기 게이팅**: `Pattern`/`YarnCatalog`는 조회·검색·매칭은 게스트도 자유롭게 가능하지만, 신규 등록·수정은 로그인한 계정만 가능 — 익명 게스트가 커뮤니티 전체가 보는 데이터를 스팸/오염시키는 걸 방지
- **수정 권한**: 등록한 사람(`createdByUserId`)만 해당 항목을 수정 가능 — "아무나 수정 가능"했던 이전 정책을 대체, 편집 충돌 문제도 함께 해결됨. Ravelry에서 가져온 항목을 처음 캐싱한 사람이 그 항목의 소유자가 됨

### 2.10 초기 시드 데이터
- 대량 API 수집이 아니라, **관리자가 직접 Ravelry를 훑어보고 고른 인기 도안·실 브랜드 20~30개 정도**를 정성껏 큐레이션
- 넣는 방법은 두 가지 다 가능: (1) 관리자 계정으로 앱을 직접 써서 일반 등록 화면을 통해 등록 — 코드 추가 불필요, (2) Prisma seed 스크립트로 한 번에 삽입 — 배포 초기 여러 건을 빠르게 넣을 때 편리 (**1단계에서는 이 방식 채택**)
- 어느 경로든 `createdByUserId`는 실제 관리자 `User.id`로 채워져서, 소유자 없는 데이터가 생기지 않고 나중에 관리자 본인이 직접 수정 가능
- 대량 미러링이 아닌 소수·수동 큐레이션이라 "사용자가 선택한 항목만 캐싱"이라는 기존 Ravelry 데이터 정책과 어긋나지 않음

### 2.11 트래커 통합 — 결정 변경 (간소화 버전으로 진행)
- 애초에 보류했던 이유는 (1) 반복 무늬별 카운터의 설계 충돌, (2) 검증 안 된 리텐션 가설, (3) 기존 원칙을 근거 없이 뒤집는 것에 대한 우려였음
- 이후 **반복 무늬별 카운터 자체를 스코프에서 제외**하기로 하면서 (1)번 문제가 해소됨 — 프로젝트 전체 단일 카운터만 쓰는 훨씬 단순한 버전으로 재설계 (2.14 참고)
- (2), (3)번 우려는 여전히 유효 — 실사용 데이터 없이 추가하는 건 동일하므로, 성공 지표(2.13)로 효과를 반드시 검증할 것

### 2.12 리텐션 훅 — 실 소진 처리
- 실 상세에서 "다 썼어요" 토글 — 소진 처리하면 스태시 목록 기본 화면에서 빠지고 "소진됨" 필터에서만 조회 가능
- 삭제와 다른 점: 기록(구매 이력, 어떤 도안에 썼는지 등)은 남기고, 그냥 "지금 활성 재고가 아님"만 표시 — 나중에 소비 이력 통계로 확장할 여지를 남겨둠
- 알림 기반 훅(찜한 도안 매칭 변경 알림, 오래 방치된 실 리마인드)은 푸시 인프라가 필요해 1단계 스코프에서 제외, 실사용 데이터를 보고 필요성이 확인되면 2단계에서 추가 검토

### 2.13 성공 지표 (가설 검증용)
- 실 등록 건수 / 무게 카테고리 채움률(선택값이라 실제로 얼마나 채우는지)
- 도안 매칭 결과 클릭률 (매칭이 실제로 유용한 정보인지)
- 재방문율 — 특히 실 소진 처리, 프로젝트 트래커 도입 전후 비교
- 이 지표들이 실제 데이터로 쌓이기 전까지는 게이지·로트 인식 같은 정교화 기능의 효과를 판단할 근거가 없다는 점을 인지하고, 추가 확장은 이 지표를 본 뒤 결정

### 2.14 프로젝트 트래커 (간소화 버전)
- 예전 뜨개 프로젝트 트래커에서 설계 충돌이 났던 **반복 무늬별 카운터는 넣지 않음** — 조각별/반복별 분기 없이 프로젝트 전체에 진행 카운터 하나만 둠
- 도안 상세의 "이 도안으로 시작하기" 버튼으로 진입 → 실 선택(선택, 매칭된 실 목록에서 고르거나 나중에 연결) → 프로젝트 생성
- 프로젝트 상세: 단수 카운터(+1/-1/리셋), 상태(진행중/완료/보류), 진행 사진 여러 장, 메모
- 완료 처리해도 실은 자동으로 소진 처리되지 않음 — 사용자가 실 상세에서 별도로 "다 썼어요" 판단 (프로젝트 하나 끝났다고 실이 다 소진됐다는 보장이 없어서 자동 연동 안 함)
- 도안 하나당 진행 중인 프로젝트가 이미 있으면, "이 도안으로 시작하기"를 다시 눌러도 새로 안 만들고 기존 프로젝트로 이동
- 하단 탭바에 "프로젝트" 탭 추가 (실/도안/프로젝트/마이) — 재방문을 유도하는 핵심 진입점이라 별도 탭으로 노출

---

## 3. 전체 앱 구조 (하단 탭바)

도안이 실에 종속되지 않는 독립된 엔티티로 자리잡도록 탭을 분리합니다.

| 탭 | 내용 |
|---|---|
| 실 | 스태시 목록, 실 등록, 실 상세(보유 현황 · 도안 추천) |
| 도안 | 도안 목록(전체/찜함 필터, 도구 종류 필터), 검색+등록(로컬+Ravelry 통합), 도안 상세(역매칭 포함) |
| 프로젝트 | 진행 중인 프로젝트 목록, 프로젝트 상세(단수 카운터·상태·사진·메모) |
| 마이 | 로그인 상태 표시, 게스트인 경우 로그인 유도, 로그아웃 |

---

## 4. 데이터 모델 (초안)

```prisma
enum WeightCategory {
  LACE
  FINGERING
  SPORT
  DK
  WORSTED
  ARAN
  BULKY
  SUPER_BULKY
}

enum AuthProvider {
  KAKAO
  GOOGLE
  GUEST
}

enum PatternSource {
  RAVELRY   // 공식 API로 실시간 조회·캐싱, 완전히 통합된 소스
  LINK      // 출처 URL은 있으나 API 연동은 안 한 것 (블로그, Etsy, LoveCrafts 등)
  USER      // 링크 없이 순수 직접 입력
}

enum CatalogSource {
  RAVELRY
  USER
}

enum CraftType {
  KNITTING // 대바늘
  CROCHET  // 코바늘
  BOTH     // 대바늘+코바늘
}

enum UnitSystem {
  METRIC
  IMPERIAL
}

model User {
  id         String       @id @default(cuid())
  provider   AuthProvider
  providerId String?      // 게스트는 null, 소셜 로그인은 카카오/구글 고유 ID
  nickname   String?
  createdAt  DateTime     @default(now())

  yarns      Yarn[]
  bookmarks  PatternBookmark[]
  createdPatterns     Pattern[]
  createdYarnCatalogs YarnCatalog[]
  projects            Project[]

  @@unique([provider, providerId])
}

model YarnCatalog {
  id             String          @id @default(cuid())

  createdByUserId String
  createdByUser   User           @relation(fields: [createdByUserId], references: [id])

  brand          String
  lineName       String
  fiber          String?
  weightCategory WeightCategory?
  needleSize     String?
  gaugeStitches  Float?          // 표준 게이지, 콧수/10cm (제조사 권장 기준)
  sourceType     CatalogSource
  ravelryId      Int?            @unique
  thumbnailUrl   String?
  createdAt      DateTime        @default(now())

  yarns          Yarn[]
  originalOfPatterns Pattern[]

  @@index([brand])
  @@index([lineName])
}

model Yarn {
  id            String         @id @default(cuid())

  userId        String
  user          User           @relation(fields: [userId], references: [id])

  // 어떤 표준 실 정보에서 파생됐는지 (자동완성 선택 시 채워짐, 직접 입력 시 null)
  catalogId     String?
  catalog       YarnCatalog?   @relation(fields: [catalogId], references: [id])

  brand         String
  lineName      String?
  colorName     String?
  fiber         String?
  weightCategory WeightCategory? // 선택 — 모르면 비워두고 등록 가능, 대신 도안 추천에서 제외됨
  needleSize    String?
  gaugeStitches Float?         // 게이지, 콧수/10cm (카탈로그 자동완성 시 채워짐, 직접 입력도 가능, 선택)
  memo          String?        // 개인 메모 (자유 텍스트)
  consumed      Boolean        @default(false) // 소진 처리 여부 — true면 기본 목록에서 제외, 기록은 유지
  createdAt     DateTime       @default(now())

  photos        YarnPhoto[]
  batches       YarnBatch[]
  projects      Project[]
}

model YarnPhoto {
  id        String   @id @default(cuid())
  yarnId    String
  yarn      Yarn     @relation(fields: [yarnId], references: [id], onDelete: Cascade)
  url       String
  publicId  String?  // Cloudinary public_id (삭제/변환용)
  createdAt DateTime @default(now())
}

model YarnBatch {
  id             String    @id @default(cuid())
  yarnId         String
  yarn           Yarn      @relation(fields: [yarnId], references: [id], onDelete: Cascade)

  dyeLot         String?   // 염색로트, 없으면 null (단순 합계로 취급)
  skeinCount     Int
  weightPerSkeinG Float    // 정규화된 값 (내부 저장 기준)
  lengthPerSkeinM Float    // 정규화된 값 (내부 저장 기준)
  inputUnit      UnitSystem // 사용자가 입력했던 단위 (표시용)
  purchasedAt    DateTime?
  createdAt      DateTime  @default(now())
}

model Pattern {
  id                 String            @id @default(cuid())

  createdByUserId    String
  createdByUser      User              @relation(fields: [createdByUserId], references: [id])

  name               String
  designer           String?
  craftType          CraftType         // 필수 — 대바늘/코바늘/대바늘+코바늘
  weightCategory     WeightCategory    // 필수 — 매칭 쿼리의 핵심 조건이라 nullable 불허
  requiredMinM       Float             // 필수 — 정규화된 필요량 최소값(m), 사이즈 여러 개인 도안의 가장 작은 사이즈 기준
  requiredMaxM       Float?            // 정규화된 필요량 최대값 (선택, 범위형 도안일 때만 채워짐)
  requiredUnit       UnitSystem?       // 사용자가 입력했던 단위 (표시용)
  gaugeStitches      Float?            // 도안이 요구하는 게이지, 콧수/10cm (선택)
  sourceUrl          String?           // 참고 링크 (LINK 소스일 때 사실상 필수, USER는 선택)
  sourceType         PatternSource
  ravelryId          Int?              @unique
  thumbnailUrl       String?
  createdAt          DateTime          @default(now())

  // 원본 실 (디자이너가 사용한 실) — YarnCatalog 재사용
  originalYarnCatalogId String?
  originalYarnCatalog   YarnCatalog?   @relation(fields: [originalYarnCatalogId], references: [id])
  originalYarnBrand     String?        // 자동완성 선택 또는 직접 입력값 보관
  originalYarnLine      String?

  bookmarks          PatternBookmark[]
  projects           Project[]

  @@index([weightCategory])
  @@index([craftType])
  @@index([name])
  @@index([designer])
}

model PatternBookmark {
  id        String   @id @default(cuid())
  userId    String
  user      User     @relation(fields: [userId], references: [id])
  patternId String
  pattern   Pattern  @relation(fields: [patternId], references: [id], onDelete: Cascade)
  memo      String?  // 개인 메모 (자유 텍스트, 다른 유저에게 노출 안 됨)
  createdAt DateTime @default(now())

  @@unique([userId, patternId])
}

enum ProjectStatus {
  IN_PROGRESS
  COMPLETED
  ON_HOLD
}

model Project {
  id        String        @id @default(cuid())
  userId    String
  user      User          @relation(fields: [userId], references: [id])

  patternId String
  pattern   Pattern       @relation(fields: [patternId], references: [id])
  yarnId    String?       // 선택 — 나중에 연결하거나 끝까지 안 정해도 됨
  yarn      Yarn?         @relation(fields: [yarnId], references: [id], onDelete: SetNull)

  status     ProjectStatus @default(IN_PROGRESS)
  currentRow Int           @default(0) // 반복 무늬별 분기 없이 프로젝트 전체 단일 카운터
  memo       String?
  createdAt  DateTime      @default(now())
  updatedAt  DateTime      @updatedAt

  photos ProjectPhoto[]

  // 주의: userId+patternId 유니크 제약을 걸지 않음 — 완료된 프로젝트를 다시 시작(같은 도안 재작업)할 수 있어야 하므로
  // "이미 진행 중인 프로젝트가 있으면 새로 안 만들고 기존으로 이동"은 status=IN_PROGRESS 조건까지 포함해 애플리케이션 레벨에서 체크
}

model ProjectPhoto {
  id        String   @id @default(cuid())
  projectId String
  project   Project  @relation(fields: [projectId], references: [id], onDelete: Cascade)
  url       String
  publicId  String?
  createdAt DateTime @default(now())
}
```

**설계 메모**
- `Yarn`은 마스터 정보만, 실제 수량은 `YarnBatch`에서 관리 → 로트 구분 여부를 사용자가 자유롭게 선택
- `YarnCatalog`는 Pattern/Ravelry 하이브리드 검색 구조를 그대로 재사용 — `Yarn`이 자체 값을 보관하되 `catalogId`로 느슨하게 참조해서, 카탈로그가 나중에 수정/병합돼도 기존 등록 기록엔 영향 없음
- 총 보유 야드 = `SUM(batch.skeinCount * batch.lengthPerSkeinM)` (조회 시점에 계산, 혹은 `Yarn`에 비정규화 캐시 필드 추가 검토 가능)
- `Pattern.weightCategory`/`requiredMinM`은 매칭 쿼리의 핵심 조건 — Ravelry API 응답의 야드 정보를 매핑해 채움(도안이 여러 사이즈를 지원하면 `yardage`~`yardage_max` 범위가 오는 경우가 많아 `requiredMinM`/`requiredMaxM` 둘 다 저장), 직접 등록 시에는 입력한 단위를 m으로 정규화
- 매칭 비율(%) 계산은 항상 `requiredMinM`(가장 작은 사이즈 기준) 대비 보유량으로 계산 — "이 실로 최소한 어떤 사이즈는 뜰 수 있는지"를 보여주는 게 목적이라 가장 포용적인 해석 채택. `requiredMaxM`이 있으면 카드에 "850~1200m 필요" 형태로 범위를 같이 표시
- **게이지 비교**: `|Yarn.gaugeStitches - Pattern.gaugeStitches| <= 1`이면 "게이지 일치", 초과하면 "게이지 차이 있음" 칩 표시. 둘 중 하나라도 값이 없으면 비교 자체를 생략 (하드 필터가 아니라 정보성 보조 지표라, 없는 데이터를 억지로 요구하지 않음)
- **로트 인식**: `YarnBatch.dyeLot`별로 그룹핑해 로트별 합계를 구하고(dyeLot이 null인 배치들은 하나의 그룹으로 합산), 가장 큰 로트 그룹의 합계가 `requiredMinM` 미만이면 "로트를 섞어야 해요" 안내 추가 — 전체 합계 기준 매칭 비율(%)은 그대로 유지하고, 이건 보조 안내로만 얹음
- `Pattern.originalYarnCatalogId`도 `YarnCatalog`를 느슨하게 참조하는 동일한 하이브리드 패턴 — 카탈로그가 나중에 병합/수정돼도 도안에 적힌 원본 실 정보는 유지
- 단위 변환: `1oz = 28.35g`, `1yd = 0.9144m` 고정 상수로 내부 정규화
- `Yarn`/`PatternBookmark`에만 `userId`를 둬서 개인 소유 데이터로 취급하고, `Pattern`/`YarnCatalog`는 전체 유저가 공유하는 데이터라 `userId`가 없음
- 게스트도 `User` 테이블의 행 하나 (`provider: GUEST`, `providerId: null`) — 로그인 시점에 게스트 `User.id`로 걸려있던 `Yarn`/`PatternBookmark`의 `userId`를 새 계정 `id`로 일괄 업데이트하면 병합이 끝남
- `Pattern`/`YarnCatalog`의 `createdByUserId`는 "누가 처음 등록/캐싱했는지"를 남겨 수정 권한을 판별하는 용도 — 소유권(`userId`)과는 다른 개념으로, 데이터 자체는 여전히 전체 공유
- `Yarn`은 사진을 여러 장 첨부할 수 있도록 `photoUrl` 단일 필드 대신 `YarnPhoto[]` 관계로 분리 (트래커 프로젝트의 `ProjectPhoto`와 동일한 패턴)
- `Yarn`/`Pattern` 삭제는 `onDelete: Cascade`로 하위 데이터(배치, 사진, 찜)까지 함께 정리 — 소프트 삭제 없이 하드 삭제로 시작
- 도안 삭제 가능 여부는 `COUNT(PatternBookmark WHERE patternId = X AND userId != 소유자) = 0 AND COUNT(Project WHERE patternId = X) = 0` 두 조건을 함께 판별 — 찜도 없고 진행 중인 프로젝트로도 참조되지 않을 때만 삭제 버튼 활성화
- `Project`는 `userId`+`patternId` 유니크 제약을 걸지 않음 — 완료 후 같은 도안을 다시 시작(재작업, 선물용 재제작 등)할 수 있어야 하므로, "이미 진행 중인 프로젝트가 있으면 새로 안 만듦" 체크는 `status = IN_PROGRESS` 조건까지 포함해서 애플리케이션 레벨(서비스 로직)에서 수행
- `Project.yarn` 관계는 `onDelete: SetNull` — 연결된 실이 삭제돼도 프로젝트 자체는 안 사라지고 실 연결만 해제됨
- 매칭 쿼리(실→도안, 도안→실 양방향 모두)는 `Yarn.consumed = false` 조건을 반드시 포함 — 소진 처리된 실이 계속 추천되는 모순을 방지
- `Pattern.weightCategory`/`requiredMinM`은 `Yarn`과 달리 non-null 필수값 — 실은 "모르면 비워두고 매칭에서만 빠짐"이 허용되지만, 도안은 이 값이 없으면 매칭 자체의 존재 이유가 없어지므로 필수로 강제
- `Pattern.craftType`은 매칭 로직(무게 카테고리·야드 비교)엔 관여하지 않음 — 순수하게 필터/표시 용도. Ravelry API 응답의 `craft` 필드를 매핑해서 채우고, 직접 등록 시엔 사용자가 선택
- 도안 등록 폼의 `craftType`/`weightCategory`처럼 전체 공유 데이터를 좌우하는 필수 셀렉트는 기본값을 미리 선택해두지 않음 — 무심코 제출해서 잘못된 값이 커뮤니티 데이터를 오염시키는 걸 막기 위해 명시적 선택을 강제 (같은 필드라도 `Yarn` 쪽은 개인 데이터라 "선택 안 함" 허용, `Pattern` 쪽은 공유 데이터라 반드시 값을 고르게 함 — 파급력 차이에 따른 처리)
- `YarnCatalog` 캐싱은 `ravelryId` 기준 find-or-create(upsert)로 처리해 여러 유저가 동시에 같은 실을 처음 캐싱해도 중복 row가 안 생기게 함 — 다만 Ravelry 연결 없이 순수 직접 입력된 카탈로그 항목은 현재 중복 방지 수단이 없다는 한계가 있고, 실사용 중 중복이 실제로 문제 되면 `@@unique([brand, lineName])` 추가를 고려
- 최종 점검 시 느슨한 문자열 필드들을 모두 enum으로 통일 — `YarnCatalog.source` → `sourceType: CatalogSource`, `YarnBatch.inputUnit`/`Pattern.requiredUnit` → `UnitSystem`. 오타로 인한 데이터 불일치를 막고, Prisma 마이그레이션 시점에 값 종류가 코드로 명확히 드러나도록 함
- `Pattern`에 `name`/`designer` 인덱스 추가 — 도안 자동완성 검색(하이브리드)이 가장 빈번한 쿼리라 성능상 필수

---

## 5. 다음 단계 후보

- [x] Prisma 스키마 최종 확정 (enum 통일, 인덱스 정리 완료)
- [x] Ravelry API의 yardage/weight 필드 실제 응답 구조 확인 — 범위형 야드(`requiredMinM`/`requiredMaxM`), ARAN 카테고리 반영 완료
- [x] 로그인/게스트 시작 화면을 프로토타입에도 구현 (문서-코드 간극 해소)
- [x] 기획서·화면설계서·스키마 3자 정합성 점검 (Project 유니크 제약, Pattern 필수값, 소진 실 매칭 제외, 삭제 시 연결된 프로젝트 체크 등 수정 완료)
- [x] 프로젝트 트래커를 프로토타입에도 반영
- [ ] 관리자 계정 생성 및 Prisma seed 스크립트 작성 (관리자가 큐레이션한 도안·실 카탈로그 20~30개)
- [ ] 프로토타입의 `useState` 기반 화면 전환을 React Router로 교체 (URL 딥링크, 브라우저 뒤로가기 지원)
