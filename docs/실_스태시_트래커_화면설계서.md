# 실 스태시 트래커 화면설계서

기획서(`실_스태시_트래커_기획서.md`)의 데이터 모델을 기준으로 각 화면을 상세화한 문서입니다.

---

## 0. 디자인 시스템

뜨개 트래커 프로젝트에서 확정한 톤을 그대로 이어갑니다 — 배경 아이보리 `#FAF6EE`, 카드 화이트, 포인트 테라코타 `#C9663C`, 서브 세이지그린 `#7C8B6F`, Pretendard 폰트.

---

## 0-1. 시작 화면 (로그인 / 게스트 선택)

**목적**: 기기에 세션이 없는 최초 실행 시, 로그인과 게스트 중 하나를 명시적으로 고르게 함

**구성**
- 서비스 로고/이름
- `카카오로 로그인`, `구글로 로그인` 버튼
- `게스트로 시작하기` 버튼 (보조 버튼, 로그인 버튼들과 시각적으로 구분)
- 게스트로 시작해도 나중에 "마이" 탭에서 언제든 로그인 가능하다는 점을 짧은 안내 문구로 표시

**상태**
- 세션 토큰이 이미 있는 기기: 이 화면을 건너뛰고 바로 메인(홈)으로 진입

**연결 데이터**: 게스트 선택 시 `POST /auth/guest` (익명 User 생성 + JWT 발급), 로그인 선택 시 각 OAuth 플로우 → `POST /auth/kakao` 또는 `POST /auth/google` — 발급된 JWT는 쿠키가 아닌 **localStorage**에 저장하고 이후 모든 요청에 `Authorization: Bearer` 헤더로 첨부 (도메인이 분리된 배포 구조상 쿠키보다 안정적)

---

## 0-2. 전체 앱 구조 (하단 탭바)

도안이 실에 종속되지 않는 독립된 엔티티로 자리잡도록 탭을 분리합니다.

| 탭 | 내용 |
|---|---|
| 실 | 스태시 목록, 실 등록, 실 상세(보유 현황 · 도안 추천) |
| 도안 | 도안 목록(전체/찜함 필터), 검색, 등록, 도안 상세(역매칭 포함) |
| 프로젝트 | 진행 중인 프로젝트 목록, 프로젝트 상세(단수 카운터·상태·사진·메모) |
| 마이 | 로그인 상태 표시, 게스트인 경우 로그인 유도, 로그아웃 |

- 등록/상세처럼 깊이 들어간 화면에서는 탭바를 숨기고 뒤로가기로 복귀

---

## 1. 실 목록 (실 탭)

**목적**: 보유한 실 전체를 한눈에 파악

**구성**
- 상단: 무게 카테고리 필터 칩 (전체/레이스/피어링/DK/워스티드/벌키 등 가로 스크롤)
- 검색창: 브랜드·라인명·색상명으로 실시간 필터링 (도안 탭 검색과 동일한 톤)
- 정렬 토글: `최근 등록순` / `이름순` (브랜드+라인명 가나다·알파벳순)
- "소진됨 보기" 토글 — 기본은 꺼짐(활성 재고만 표시), 켜면 소진 처리한 실도 함께 표시 (흐리게/배지로 구분)
- 우상단: `+` 실 등록 버튼
- 실 카드 그리드 (2열)
  - 카드: 대표 사진(첫 번째 등록 사진), 브랜드+실 이름, 색상명, 총 보유량(`420m · 3타래` 형태로 배치 합산 표시)
  - 로트가 여러 개인 경우 카드 우하단에 작은 로트 개수 배지(`로트 2개`)
  - 소진 처리된 실은 카드에 "소진됨" 배지 + 흐리게 표시

**상태**
- 빈 상태(실 0개): "첫 실을 등록해보세요" 안내 + 등록 버튼 강조
- 필터 결과 0개: "해당 굵기의 실이 없어요"
- 검색 결과 0개: "일치하는 실이 없어요"

**연결 데이터**: `GET /yarns?q=&weightCategory=&sort=&includeConsumed=` (batches 합산값 포함 응답)

---

## 2. 실 등록

**목적**: 실 마스터 정보와 최소 1개의 보유 기록(배치)을 함께 등록

**구성 — 마스터 정보**
- 사진 업로드 (여러 장 가능, 썸네일 행으로 나열되고 각 사진에 삭제 버튼)
- 브랜드 또는 라인명 입력창 — 타이핑 중 실시간 추천 드롭다운 노출 (도안 자동완성과 동일한 UX)
  - 항목: 브랜드+라인명(굵게) / 소재·무게 카테고리(회색 작은 글씨, "내 DB" 또는 "Ravelry")
  - 클릭 시 브랜드/소재/무게 카테고리/추천 바늘 사이즈/게이지 자동 채움 + "◯◯에서 자동 입력됨" 표시, `catalogId` 연결
  - 클릭 없이 계속 입력하면 자유 텍스트로 취급, `catalogId`는 null로 남음
- 색상명 (직접 입력, 자동완성 대상 아님)
- 소재, 무게 카테고리, 추천 바늘 사이즈, 게이지(콧수/10cm, 선택) — 자동 채움 후에도 수정 가능
- 메모 (선택, 자유 텍스트 — 예: "선물로 받음", "세일 때 구매")

**구성 — 보유 기록(배치)**
- 단위 선택 토글: `g·m` / `oz·yd` — 선택에 따라 아래 입력 라벨만 바뀜, 저장 시 내부적으로 g·m로 정규화
- 타래당 무게, 타래당 길이, 보유 타래 수 입력
- 염색로트(선택): 비워두면 "로트 구분 없음"으로 단순 합산 취급
- `+ 다른 로트 추가` 버튼으로 배치를 여러 개 등록 가능 (등록 시점에 한 번에, 또는 실 상세에서 나중에 추가도 가능 — 아래 3번 참고)
- 구매일 (선택, 날짜 입력)

**상태**
- 무게 카테고리 미선택 시 도안 추천 탭에서 매칭이 불가능하다는 안내 문구 (선택 유도, 강제는 아님)

**연결 데이터**: `GET /yarn-catalog/search?q=` (로컬 우선, 부족 시 Ravelry 폴백), 선택 시 로컬에 없던 항목은 최소 메타데이터로 `YarnCatalog`에 캐싱. 등록 제출은 `POST /yarns` (마스터 + `catalogId`(선택) + 메모 + 사진 URL 목록 + 최초 배치 1개 이상 포함)

---

## 3. 실 상세

**목적**: 특정 실의 보유 현황을 확인하고, 도안 추천으로 이어지는 진입점

**구성**
- 상단: 사진 캐러셀(여러 장 스와이프), 브랜드+실 이름, 색상, 소재, 무게 카테고리 배지, 우측 상단 연필(수정) + 휴지통(삭제) 아이콘
- 메모가 있으면 배지 아래 표시
- **"다 썼어요" 토글**: 배지 영역 근처에 배치. 켜면 소진 처리되어 실 목록 기본 화면에서 빠짐 — 삭제와 달리 기록은 그대로 유지, 다시 끄면 목록에 복귀
- 탭 2개: `보유 현황` / `이 실로 뜰 수 있는 도안`
- 휴지통 아이콘 탭 시 "삭제하면 되돌릴 수 없어요" 확인 모달, 확인하면 배치·사진까지 함께 삭제되고 실 목록으로 이동

**탭 1 — 보유 현황**
- 배치 목록 (로트별 카드): 로트 번호(또는 "로트 구분 없음"), 타래 수, 타래당 무게·길이, 구매일
- 합계 요약: 총 타래 수, 총 무게, 총 길이
- `+ 배치 추가` 버튼 — 같은 실을 새로 구매했을 때 기록 추가
- 배치 카드 탭 시 인라인 수정 모드로 전환 (로트 번호, 타래 수, 타래당 무게·길이 수정), 별도 삭제 버튼 유지

**탭 2 — 이 실로 뜰 수 있는 도안**
- 소진 처리(`consumed: true`)된 실은 이 탭 자체가 매칭을 실행하지 않음 (다 쓴 실이 계속 추천되는 걸 방지)
- 진입 시 자동으로 매칭 쿼리 실행 (무게 카테고리 일치, 필요 야드 최소값 대비 보유 야드 비율 계산)
- 결과 카드: 도안명, 작가, 필요 야드(범위가 있으면 "850~1200m" 형태), 보유 야드 대비 여유분(`요구량 대비 130% 보유` 형태, 최소값 기준), 우측에 하트(찜) 아이콘
- 여유분 기준: 130% 이상 "여유 있음" / 110~130% "충분함" / 100~110% "타이트함"(경고 색상) / 100% 미만 "부족함"(강조 색상) — 100% 미만도 숨기지 않고 함께 보여줌 (비율 내림차순 정렬이라 자연스럽게 하단에 위치)
- **게이지 보조 칩**: 실·도안 둘 다 게이지 정보가 있을 때만 "게이지 일치" / "게이지 차이 있음" 작은 칩 추가 표시 (허용 오차 ±1콧수)
- **로트 보조 안내**: 합계는 충분한데 최대 단일 로트로는 부족하면 카드 하단에 "로트를 섞어야 해요" 문구 추가

**상태**
- 무게 카테고리 미입력: 탭 진입 시 "무게 카테고리를 입력하면 추천받을 수 있어요" 안내, 실 정보 수정으로 유도
- 매칭 결과 0건(같은 무게 카테고리의 도안 자체가 없음): "같은 굵기의 도안을 찾지 못했어요, 로컬 DB에 등록된 도안이 아직 적어서일 수 있어요" + Ravelry에서 더 찾아보기 링크
- 로딩: 카드 스켈레톤

**연결 데이터**: `GET /yarns/:id`, `GET /yarns/:id/pattern-matches`, `PATCH /yarns/:id` (`consumed` 토글 포함), `DELETE /yarns/:id` (실 삭제)

---

## 3-1. 실 수정

**목적**: 실 마스터 정보와 배치 기록을 나중에 바로잡을 수 있게 함

**구성**
- 실 상세 상단 연필 아이콘으로 진입
- 등록 화면과 동일한 필드 구성(브랜드/라인명/색상/소재/무게 카테고리/바늘 사이즈/사진 여러 장/메모), 현재 값으로 미리 채워짐 — 카탈로그 자동완성 검색은 다시 띄우지 않고 필드 직접 수정만 지원 (이미 등록된 항목이라 검색보다 직접 수정이 자연스러움)
- 사진은 기존 사진 썸네일 목록(개별 삭제 가능) + 추가 업로드
- 배치는 실 상세의 `보유 현황` 탭에서 인라인으로 수정 (별도 화면 아님, 3번 화면 참고)
- 실 자체 삭제는 이 화면이 아니라 실 상세(3번 화면)의 휴지통 아이콘에서 처리 — 수정은 정보 고치기에만 집중

**연결 데이터**: `PATCH /yarns/:id` (마스터 정보, 사진 목록, 메모), `PATCH /yarns/:id/batches/:batchId` (배치별 인라인 수정)

---

## 4. 도안 목록 (도안 탭)

**목적**: 등록/찜한 도안 전체를 독립적으로 관리하는 홈

**구성**
- 상단 필터: `전체` / `찜함`
- 도구 종류 필터 칩: `전체` / `대바늘` / `코바늘` / `대바늘+코바늘`
- 검색창: 도안명·작가로 검색 (하이브리드: 우리 DB 우선 → Ravelry 폴백)
- 카드 리스트: 도안명, 작가, 필요 야드(범위 표시), 무게 카테고리 배지, 도구 종류 배지, 출처, 우측 하트(찜) 아이콘
- 검색 결과가 없을 때: "찾는 도안이 없나요?" + `등록하기` 버튼으로 5번 화면 진입 (직접 입력 필드로 바로 스크롤)
- 우상단 `+` 버튼으로도 5번 화면(검색부터 시작) 진입 가능

**상태**
- 빈 상태(전체 필터, 등록/찜 0건): "아직 등록된 도안이 없어요" + 검색·등록 유도
- 찜함 필터인데 찜한 게 없을 때: "아직 찜한 도안이 없어요"
- 도구 종류 필터 결과 0개: "해당하는 도안이 없어요"

**연결 데이터**: `GET /patterns?filter=all|bookmarked&craftType=`, `GET /patterns/search?q=`, 찜 토글은 `POST/DELETE /patterns/:id/bookmark`

---

## 5. 도안 등록 (로컬 + Ravelry 통합 검색)

**목적**: 도안명/작가를 검색해서 우리 DB와 Ravelry 결과를 한 번에 확인하고, 있으면 상세로 이동·없으면 새로 등록

**구성**
- 상단 검색창: 타이핑 중 실시간 추천 드롭다운 노출, 우리 DB 항목과 Ravelry 항목을 함께 표시 (출처 라벨로 구분)
  - 이미 등록된(우리 DB) 항목 클릭 → 바로 도안 상세로 이동, 폼 입력 없이 종료
  - Ravelry 항목 클릭 → 도안명/작가/도구 종류/무게 카테고리/필요량(범위가 있으면 최소·최대 둘 다)이 자동 채워짐 + "Ravelry에서 자동 입력됨" 표시
  - 검색 결과가 없으면 아래 필드를 직접 입력해서 신규 등록
- 도안명 (필수)
- 작가 (선택)
- 도구 종류 (필수, 드롭다운 — 대바늘 / 코바늘 / 대바늘+코바늘)
- 무게 카테고리 (필수, 드롭다운 — 매칭 기능이 이 값에 의존)
- 필요량 (최소값 필수, 최대값 선택) — 단위 토글 `m` / `yd` 선택 후 숫자 입력, 사이즈별로 필요량이 다른 도안은 최대값도 함께 입력. 내부적으로 m 기준 정규화 (실 배치 등록과 동일한 원칙)
- 게이지 (선택, 콧수/10cm) — 게이지 매칭 보조 표시에 사용, 모르면 비워둬도 됨
- 원본 실 (선택) — 브랜드/라인명 검색창, 실 등록과 동일한 자동완성 UX (`YarnCatalog` 재사용). 클릭 시 `originalYarnCatalogId` 연결, 직접 입력하면 텍스트만 보관
- 참고 링크 (선택 — 블로그·SNS 등 원본 출처)
- 도안 서술 내용(뜨기 방법, 차트 이미지 등)은 입력 필드 자체를 두지 않음 — 저작권 원칙상 사실 정보만 수집

**상태**
- 필수값 미입력 시 등록 버튼 비활성화 — 도구 종류·무게 카테고리 드롭다운은 값을 미리 선택해두지 않고 "선택해주세요" 상태로 시작, 사용자가 명시적으로 고르기 전까진 제출 불가 (공유 데이터를 오염시킬 수 있는 필드라 무심코 기본값 그대로 제출되는 걸 막기 위함)
- 게스트(비로그인) 상태에서 등록 버튼을 누르면 로그인 유도 화면으로 전환 — 검색/조회까지는 게스트도 그대로 가능

**연결 데이터**: `GET /patterns/search?q=` (로컬 우선 + Ravelry 병합 응답), 등록 제출은 `POST /patterns` (Ravelry 선택 시 `sourceType: RAVELRY` + `ravelryId` 포함, 참고 링크 입력 시 `sourceType: LINK`, 링크 없이 직접 입력 시 `sourceType: USER`)

---

## 6. 도안 상세

**목적**: 도안 정보를 확인하고, 보유 실과의 매칭을 양방향으로 확인

**구성**
- 도안명, 작가, 출처(내 DB/Ravelry) 표시, 우측 상단 하트(찜) 토글 + 연필(수정) + 휴지통(삭제) 아이콘
- **본인 외에 다른 유저가 찜했거나, 이 도안으로 진행 중인 프로젝트가 있으면 휴지통 아이콘이 비활성화**되고 탭하면 사유에 맞는 안내("다른 분들이 찜한 도안이라 삭제할 수 없어요" 또는 "진행 중인 프로젝트가 있어 삭제할 수 없어요") 표시. 둘 다 없다면 탭 시 "삭제하면 되돌릴 수 없어요" 확인 모달
- 필요량(m 환산, 범위가 있으면 "850~1200m" 형태로 표시) / 무게 카테고리 / 도구 종류(대바늘·코바늘·대바늘+코바늘) 배지
- 원본 실 정보 (있는 경우) — 브랜드/라인명 칩, `YarnCatalog` 연결 시 탭하면 해당 실 카탈로그 정보로 이동
- Ravelry 원본 링크 또는 참고 링크 (출처에 따라)
- **내가 가진 실 중 맞는 것** 섹션 — 같은 무게 카테고리의 실을 모두 보여주며(소진 처리된 실은 제외), 보유 야드 대비 여유분 비율로 정렬 (실 탭과 동일한 4단계 칩: 여유 있음/충분함/타이트함/부족함, 100% 미만도 숨기지 않음). 게이지·로트 보조 칩도 동일하게 표시
- **내 메모** — 찜한 상태일 때만 노출되는 자유 텍스트 입력란(예: "5.5mm로 바꿔서 뜰 것"), 찜 해제 시 메모도 함께 사라짐
- **"이 도안으로 시작하기" 버튼** — 이미 이 도안으로 진행 중인 프로젝트가 있으면 바로 그 프로젝트 상세로 이동, 없으면 프로젝트 시작 화면(6-2)으로 이동

**상태**
- 역매칭 결과 0건(같은 무게 카테고리의 실 자체가 없음): "같은 굵기의 실이 없어요"

**연결 데이터**: `GET /patterns/:id`, `GET /patterns/:id/yarn-matches`, 찜 토글은 `POST/DELETE /patterns/:id/bookmark`, 메모 저장은 `PATCH /patterns/:id/bookmark`, `GET /patterns/:id/bookmark-count?excludeSelf=true` + `GET /projects?patternId=` (삭제 가능 여부 판별에 함께 사용), `DELETE /patterns/:id` (로그인 필요, 소유자 검증 + 타인 찜 0건 + 연결 프로젝트 0건 검증)

---

## 6-1. 도안 수정

**목적**: 등록한 사람이 도안 정보를 바로잡을 수 있게 함

**구성**
- 도안 상세 상단 연필 아이콘으로 진입 — 등록한 사람(`createdByUserId`)에게만 노출, Ravelry 출처 도안도 처음 캐싱한 사람이면 동일하게 수정 가능
- 등록·수정 자체가 로그인 필요 액션이라 소유자는 항상 로그인 계정 (게스트는 애초에 도안을 등록/수정할 수 없음)
- 등록 화면과 동일한 필드(도안명/작가/도구 종류/무게 카테고리/필요량 최소·최대+단위/원본 실/참고 링크), 현재 값으로 미리 채워짐 — 검색 UI는 다시 띄우지 않고 필드 직접 수정만 지원
- 저장 시 `ravelryId`는 참고용으로 유지, `sourceType`은 변경하지 않음 (Ravelry에서 온 도안을 수정해도 출처 자체는 Ravelry로 유지, 소유자만 바뀌지 않고 그대로)
- 도안 삭제는 이 화면이 아니라 도안 상세(6번 화면)의 휴지통 아이콘에서 처리 — 수정은 정보 고치기에만 집중

**연결 데이터**: `PATCH /patterns/:id` (로그인 필요, 소유자 검증)

---

## 6-2. 프로젝트 시작

**목적**: 도안 상세에서 "이 도안으로 시작하기"를 누르면 실을 선택(선택 사항)하고 프로젝트를 생성

**구성**
- 도안 정보 요약(도안명, 필요량)
- 실 선택 — "내가 가진 실 중 맞는 것" 목록에서 고르거나 "나중에 연결" 선택
- `시작하기` 버튼 → 프로젝트 생성(상태: 진행중, 단수 0) → 프로젝트 상세로 이동

**연결 데이터**: `POST /projects` (patternId + yarnId(선택))

---

## 7. 프로젝트 목록 (프로젝트 탭)

**목적**: 진행 중인 프로젝트를 한눈에 확인 — 재방문을 유도하는 핵심 진입점

**구성**
- 상단 필터: `전체` / `진행중` / `완료` / `보류`
- 카드 리스트: 도안명, 연결된 실(있으면), 현재 단수, 상태 배지, 마지막 업데이트 일시
- 카드 탭 시 프로젝트 상세로 이동

**상태**
- 빈 상태(진행중 필터, 프로젝트 0건): "아직 시작한 프로젝트가 없어요, 도안 탭에서 마음에 드는 도안을 찾아 시작해보세요"

**연결 데이터**: `GET /projects?status=`

---

## 7-1. 프로젝트 상세

**목적**: 진행 상황을 기록하고 업데이트

**구성**
- 상단: 도안명, 연결된 실(있으면 브랜드+라인명, 없으면 "실 연결하기" 버튼)
- 상태 토글: `진행중` / `완료` / `보류`
- **단수 카운터**: 큰 숫자 표시 + `-1` / `+1` 버튼, 길게 누르면 직접 숫자 입력, `리셋` 버튼(확인 모달 거침)
- 진행 사진: 여러 장 업로드/삭제 (실 사진과 동일한 UX)
- 메모: 자유 텍스트
- 완료 처리 시 축하 문구 정도만 표시, 실 소진 처리는 자동 연동하지 않고 "실 상세에서 직접 소진 처리할 수 있어요" 안내만 덧붙임
- 휴지통 아이콘으로 프로젝트 자체 삭제 가능 (확인 모달)

**연결 데이터**: `GET /projects/:id`, `PATCH /projects/:id` (상태·단수·메모·사진·yarnId 연결), `DELETE /projects/:id`

---

## 8. 마이 (마이 탭)

**목적**: 로그인 상태를 확인하고, 게스트는 로그인을, 로그인 유저는 로그아웃을 할 수 있는 곳

**구성**
- 게스트 상태: "게스트로 이용 중" 안내 + `카카오로 로그인` / `구글로 로그인` 버튼 (누르면 게스트 데이터가 그대로 이관된다는 안내 문구 포함)
- 로그인 상태: 닉네임, 로그인한 provider(카카오/구글) 표시, `로그아웃` 버튼

**연결 데이터**: `GET /auth/me`, 로그인은 0-1과 동일한 OAuth 플로우, `POST /auth/logout`

---

## 9. 결정 사항

- **야드 여유분 기준선**: 130%+ 여유 있음 / 110~130% 충분함 / 100~110% 타이트함 / 100% 미만 부족함 — 100% 미만도 목록에서 숨기지 않고 칩으로 구분해서 모두 표시
- **배치 삭제 정책**: 1단계에서는 "다 썼어요"와 "잘못 등록" 구분 없이 단순 삭제로 시작 (사용 이력 통계 니즈가 실제로 생기면 그때 확장)
- **스태시 앱 ↔ 트래커 앱 통합 여부**: 별개로 시작했지만, 반복 무늬별 카운터를 빼는 조건으로 간소화된 프로젝트 트래커를 스태시 앱 안에 직접 통합하기로 결정 (7, 7-1, 6-2 화면 참고)
- **도안/실 탭 분리**: 도안을 실에 종속시키지 않고 독립 탭으로 분리, 양방향 매칭(실→도안, 도안→실)을 대칭적으로 지원
